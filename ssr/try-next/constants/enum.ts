export enum Themes {
    light = "light",
    dark = "dark"
}

export enum Language {
    ch = "ch",
    en = "en",
}

export enum Environment {
    pc = "pc",
    ipad = "ipad",
    mobile = "mobile",
    none = "none",
}