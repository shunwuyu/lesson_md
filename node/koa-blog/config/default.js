const config = {
  // 启动端口
  port: 8080,

  // 数据库配置
  database: {
    DATABASE: 'koa_blog',
    USERNAME: 'root',
    PASSWORD: '849072',
    PORT: '3306',
    HOST: 'localhost'
  }
}

module.exports = config